/**
 * Actions tracked by the audit system.
 */
export enum AuditAction {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
  VERIFY = 'VERIFY',
  REJECT = 'REJECT',
  PROCESS = 'PROCESS',
}
