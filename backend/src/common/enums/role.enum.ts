/**
 * User roles in the STRATA platform.
 * Maps to the Prisma Role enum in schema.prisma.
 */
export enum Role {
  ADMIN = 'ADMIN',
  SURVEYOR = 'SURVEYOR',
  REVIEWER = 'REVIEWER',
  USER = 'USER',
}
